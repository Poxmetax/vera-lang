# T08 — hole_fill_clamp (optional)

Initial program has an unfilled typed hole `?body`. Edit the source to fill the hole with a correct clamp body so the program typechecks and runs (exit 0).

**Not claimed:** hole synthesis product / automatic fill quality score.