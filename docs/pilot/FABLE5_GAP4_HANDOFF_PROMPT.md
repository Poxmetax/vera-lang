<!--
Operator: chat paste SHORT POINTER -- CLAUDE_POINTER_GAP4_IMPLEMENT.md
Thin R2 pilot only. Not full IFC. Not FixPatch E.
STATUS: LANDED d4aebd3 [GAP4-R2-PILOT] -- lattice-math evidence ONLY; do not re-implement.
-->

# Fable 5 -- VERA GAP-4 handoff (R2 label-lattice thin pilot)

**LANDED** `d4aebd3` `[GAP4-R2-PILOT]` · [`GAP4_R2_PILOT_SLICE.md`](GAP4_R2_PILOT_SLICE.md). Campaign ACK: [`CURSOR_SYNC_ACK_GAPS_BEFORE_E.md`](CURSOR_SYNC_ACK_GAPS_BEFORE_E.md). Baseline now **50**. **Overclaim:** lattice-math only -- not IFC / R2 ergonomics / CONF-P2 label gate. This file is historical implement brief.

Canonical brief for **GAP-4 only**. Pointer: [`CLAUDE_POINTER_GAP4_IMPLEMENT.md`](CLAUDE_POINTER_GAP4_IMPLEMENT.md).

---

You are continuing **VERA**. Madis is the operator. Implement a **thin pilot** for SPEC **R2 / DP4** (unified label lattice inference ergonomics) -- the ONE novel type-system concept that still has **zero implementation evidence**.

## Why (KNOWN_GAPS)

Riskiest unvalidated SPEC claim. Building more Phase 2/3 without a thin pilot increases rework. Prefer early evidence over full IFC.

## Hard constraints

Same Fable defaults: `vera-lang/` only; no mainnet; no commit unless asked; surgical; ask before >~30 lines; unique fn names; no failing examples; honest-limits (especially: **do not claim full IFC / CONF-P2 labels gate**); soft_smoke; prove_clamp 6. Baseline **44+**. Soft authors: GAP-2 refine-pred fragment rules.

## SPEC anchors (read, do not overclaim)

- SPEC §4.2 unified label lattice; DP4 one label; inference stance / risk **R2**
- Implicit flows still **[UNVERIFIED/OPEN]** -- out of scope for this pilot
- MVP today: mainly `uses` clause; value labels `T^{...}` may be post-MVP -- pilot may be lattice math + checker stub **or** minimal surface; document which

## What YOU must do (smallest closed fragment)

1. Deliver **demonstrable evidence** that labels are not vapor: pick ONE thin shape, e.g.
   - (A) internal lattice join/meet/subsumption module + unit tests for `{untrusted,secret}` atoms, **or**
   - (B) minimal parse/typecheck of a tiny label annotation on one example path, **or**
   - (C) documented ergonomics probe with failing/passing cases -- but prefer executable code over prose-only
2. Marker e.g. `[GAP4-R2-PILOT]`
3. Slice note: `docs/pilot/GAP4_R2_PILOT_SLICE.md` with HONEST LIMITS: not full IFC; not CONF-P2 label gate; implicit flows deferred; fallback (split auth/data checkers) only if you hit R2 failure -- document
4. Update `KNOWN_GAPS.md` GAP-4: CLOSED for "zero evidence" **or** PARTIAL with clear remaining work (prefer CLOSED only if executable evidence exists)
5. Do **not** implement policies, quarantine, `infer`, or actors

## Out of scope

Full IFC; implicit flows; Gap-3/5 unless Madis combines; FixPatch E; Phase 3 MCP.

## Return (English short)

VERDICT DONE-GAP4 | PARTIAL | BLOCKED; what evidence landed; honest limits; next.

End of GAP-4 handoff.
