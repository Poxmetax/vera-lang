# vera-lang `_operator_archive` (local soft archaeology)

- **Bench fixture baks** under `bench/vera_agent_bench_v01/_operator_archive/` are **tracked** (transparency for answer-leaking / pre-restore copies).
- **Crate `src` and docs soft-patch baks** live only under `_operator_archive/crate_src_bak/` and `_operator_archive/docs_bak/` — **local-only**, gitignored; do not publish to GitHub.
